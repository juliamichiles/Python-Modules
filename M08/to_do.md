# ex0:
[X] Flake8 + mypy .
[X] Tested

# ex1:
[X] maybe fix [OK] matplotlib (unknown - Visualization Ready) 
	to get the actuall version
[X] Use some AI to double check if we match all subject requirements
[X] Flake8 + mypy .
[X] Tested

# ex2:
[X] Use some AI to double check if we match all subject requirements
[X] Flake8 + mypy .
[X] Tested

# Before pushing into intra's repo:
[ ] Don't forget to remove venvs before submition
[ ] Ensure ex2/ has its own .gitignore before submition
[ ] ex2/'s .gitignore should contain:
		.env
		__pycache__/
		matrix_env/
