[ ] create gitignore and to_be_gitignore
[ ] Work in a venv
[ ] Take notes on:
	- Pydantic + basic usage of BaseModel, Field and model_validator
	- default_factory
[ ] WHERE/WHEN WAS I SUPPOSED TO HAVE USED THE DATA GENERATORS???

# ex0:
	[X] Done + Tested
	[X] Flake8 + Mypy 

# ex1:
	[X] Done + Tested
	[X] Flake8 + Mypy 

# ex2:
	[X] Started
	[ ] Maybe test with JSON data from generator??	

# OBS:
Right now, mypy must be installed inside virtual environment an run as:
	'python -m mypy space_station.py'
If the global mypy is run, we get an error saying the module couldn't be found etc.
I added a # type: ignore comment to shut it down 
[ ] Maybe investigate if this is the correct approach
